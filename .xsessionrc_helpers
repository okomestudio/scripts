#!/bin/bash
#
# BASH helper functions
#
# Requirement: xinput
#

# extract device id from xinput output
function get_dev_id() {
  device_name="$1"
  s=$(xinput | grep "$device_name")
  s="${s#*id=}"
  dev_id="${s%%[!0-9]*}"
  echo ${dev_id}
}
