#!/bin/bash
#
# myipaddress - Get the public facing IP address of the local host.
#

set -e

remotehost=ipinfo.io
# curl -s $remotehost | sed -e 's/.*Current IP Address: //' -e 's/<.*$//'
curl $remotehost
